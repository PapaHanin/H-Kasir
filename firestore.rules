rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // POS Toko Kelontong data sync collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
